import {useNavigation} from '@react-navigation/native';

interface Payload<T> {
  payload: T;
}

// export const getNavigatePath = (screen: string, payload: Payload<{}>) => {
//   const navigation = useNavigation();
//   navigation.navigate(screen);
// };
