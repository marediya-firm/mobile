import { StyleSheet } from "react-native";
import { Colors } from "../../../constant";

export const styles = StyleSheet.create({
    main: {
        flex: 1,
        backgroundColor: Colors.white,
    },
    safeAreView: { backgroundColor: '#FFC0CB', }
})